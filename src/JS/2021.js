  (function() {
            'use strict';

            const CORRECTIONS = {
                1: "✔ MODEL ANSWER (Q1)\n\nWhere a statute is ambiguous or leads to absurdity, the Legislature may intervene through:\n\n1. Amending Legislation: The Legislature can pass an amending Act to clarify the ambiguous provision or remove the absurdity. This is the most direct remedy.\n\n2. Repeal: The Legislature may repeal the offending statute entirely and enact a new one that clearly expresses its intention.\n\n3. Interpretation Acts: Enacting general interpretation provisions (e.g., Interpretation Act) that provide default meanings for common terms, thereby reducing ambiguity.\n\n4. Delegated Legislation: The Legislature may empower the Executive to make regulations that clarify the application of the statute, subject to legislative oversight.\n\n5. Pre‑legislative Scrutiny: Establishing parliamentary committees to examine Bills thoroughly before enactment, ensuring clarity and coherence.\n\n6. Explanatory Memoranda: Requiring that every Bill be accompanied by an explanatory memorandum that sets out the policy objective and intended meaning, which courts may use as an aid to interpretation.\n\nAuthorities: The Supreme Court in A-G v. Akintola (1962) emphasised the need for clear legislative intent. The Court of Appeal in Olawoyin v. A-G (1964) affirmed that courts will adopt a purposive approach where the literal meaning leads to absurdity, but the ultimate remedy for drafting defects lies with the Legislature.",
                2: "✔ MODEL ANSWER (Q2)\n\nThe procedure by which a proposed law (Bill) becomes an Act of the National Assembly involves the following stages:\n\n1. First Reading: The Bill is introduced and its title is read. No debate occurs; the Bill is published in the official gazette.\n\n2. Second Reading: The general principles and objects of the Bill are debated. If approved, it proceeds to the committee stage.\n\n3. Committee Stage: The Bill is examined in detail by a committee of the whole House or a specialised committee. Amendments may be proposed and adopted.\n\n4. Report Stage: The committee reports back to the House, and further amendments may be made.\n\n5. Third Reading: The final version of the Bill is debated and voted upon. If passed, it is sent to the other chamber (Senate or House of Representatives) for concurrence.\n\n6. Concurrence: The other chamber follows the same stages. If amendments are made, the Bill is returned to the originating chamber for approval.\n\n7. Presidential Assent: The Bill is transmitted to the President for assent. If the President assents, it becomes an Act of the National Assembly. If the President withholds assent, the Bill may still become law if passed by a two‑thirds majority of both chambers (override).\n\n8. Commencement: The Act may come into force on the date of assent or on a date appointed by the President or the Act itself.\n\nThis procedure is set out under sections 58–59 of the Constitution of the Federal Republic of Nigeria, 1999 (as amended).",
                3: "✔ MODEL ANSWER (Q3)\n\n(a) APPELLANTS' BRIEF\n\nIN THE COURT OF APPEAL, ENUGU DIVISION\nHOLDEN AT ENUGU\n\nAPPEAL NO: CA/E/XXX/2021\n\nBETWEEN:\nCHIDINMA AGBO & MERCY EDET — APPELLANTS\nAND\nTHE STATE — RESPONDENT\n\nBRIEF OF ARGUMENT ON BEHALF OF THE APPELLANTS\n\nPRELIMINARY:\nThis is an appeal against the judgment of the High Court delivered on [date], wherein the Appellants were convicted and sentenced to death by firing squad for 'offending the sensibilities of the daughter of a high‑ranking citizen of the Federal Republic of Nigeria' by playing a Davido song near her bed.\n\nISSUE FOR DETERMINATION:\nWhether the learned trial judge was right in convicting the Appellants for an offence not known to law.\n\nARGUMENT:\nThe conviction is a nullity and amounts to a grave miscarriage of justice. Section 36(12) of the Constitution of the Federal Republic of Nigeria, 1999 (as amended) provides that 'a person shall not be convicted of a criminal offence unless that offence is defined and the penalty therefore is prescribed in a written law.'\n\nIn Aoko v. Fagbemi & Anor (1961) 1 ANLR 400, the court held that no person can be convicted of an offence that is not created by statute or recognised by customary law as a penal offence. The alleged offence of 'offending the sensibilities' of a person is not known to any written law in Nigeria. The sentence of death by firing squad is likewise unknown to law for such a vague and non‑existent offence.\n\nThe trial court therefore acted without jurisdiction. The conviction and sentence must be set aside.\n\nCONCLUSION:\nThe appeal is meritorious and should be allowed. The Appellants are entitled to their immediate release.\n\nDated this … day of … 20…\n\n________________________\n(Signed)\nCounsel to the Appellants\n\n\n(b) EFFECT OF FAILURE TO FILE BRIEFS WITHIN TIME:\n\nOrder 19 of the Court of Appeal Rules prescribes time limits for filing briefs. Failure to file the appellants' brief within the prescribed time may result in:\n\n(i) The appeal being struck out for want of prosecution;\n(ii) The court refusing to hear oral argument;\n(iii) The respondent being granted leave to file their brief out of time; or\n(iv) The court making any other order it deems just, including costs.\n\nThe court has a discretionary power to extend time under Order 19 Rule 10, but such extension is not automatic and must be supported by sufficient cause.",
                4: "✔ MODEL ANSWER (Q4)\n\n(a) AFFIDAVIT FOR OFFICIAL REPORT (max 6 paragraphs)\n\nIN THE NEW HAVEN POLICE STATION\nENUGU, ENUGU STATE\n\nAFFIDAVIT OF JOHN DOE\n\nI, JOHN DOE, a Nigerian citizen and student of the University of Nigeria, Enugu Campus, do solemnly make this affidavit and state as follows:\n\n1. That I am the deponent herein and I reside at New Hostel, UNEC, Enugu, Enugu State, and I am competent to make this affidavit.\n\n2. That on the 4th day of November, 2022, at about 8:00pm, I was walking along New Haven junction, Enugu, Enugu State, when I was accosted by three unknown armed men who robbed me of my personal belongings.\n\n3. That the items stolen include my National Identification Card, my cellphone (Samsung Galaxy A12, IMEI No. 1234567890), and my laptop (HP Pavilion, Serial No. HP123456).\n\n4. That the robbers were armed with knives and a locally made pistol, and they threatened to kill me if I resisted.\n\n5. That I reported the matter to the New Haven police station on the same day, and I was issued with a report number.\n\n6. That I make this affidavit in support of my official report and to enable the police investigate the matter and apprehend the culprits.\n\n________________________\n(Signed)\nDeponent\n\nSworn to before me on this … day of … 20…\n\n________________________\nCommissioner for Oaths\n\n\n(b) LAW LIBRARIES AND LEGAL PROFESSIONALS:\n\nLaw libraries are indispensable to the legal profession for the following reasons:\n\n(i) Research: They provide primary and secondary legal materials for research, enabling lawyers to build strong arguments.\n\n(ii) Precedent Discovery: Lawyers rely on reported judgments and law reports to find binding precedents.\n\n(iii) Continuing Legal Education: Legal professionals use law libraries to stay updated on new legislation and judicial decisions.\n\n(iv) Academic Support: Law students and academics depend on libraries for textbooks, journals, and periodicals.\n\n(v) Access to Comparative Materials: Libraries facilitate comparative legal research, essential for understanding foreign and international law.\n\n(vi) Preservation: Law libraries preserve legal heritage and historical documents for posterity.",
                5: "✔ MODEL ANSWER (Q5)\n\n(a) CONCEPTS:\n\n(i) Judicial Precedent: The doctrine whereby courts follow previous decisions of higher or coordinate courts on similar legal issues. It ensures consistency, certainty, and predictability in the law. The principle of stare decisis governs this doctrine.\n\n(ii) Ratio Decidendi: The binding part of a judicial decision—the legal principle or rule of law on which the decision is founded. It is the reason for the decision and constitutes the precedent.\n\n(iii) Obiter Dicta: Remarks or observations made by a judge in a judgment that are not necessary for the decision. They are persuasive but not binding on later courts.\n\n\n(b) NIGERIAN LEGAL SYSTEM AS AN ADMIXTURE:\n\nThe Nigerian legal system is a hybrid of three main components:\n\n(i) Received English Law: This includes the common law, doctrines of equity, and statutes of general application enacted in England before 1 January 1900. These were received through colonisation and form the foundation of Nigerian jurisprudence.\n\n(ii) Customary Law: Indigenous laws and customs of the various ethnic groups in Nigeria. They regulate personal matters such as marriage, inheritance, and land tenure. They are valid if not repugnant to natural justice, equity, and good conscience, and not incompatible with any existing law.\n\n(iii) Islamic Law (Muslim Law): Applied in the northern states and other Muslim communities, primarily in matters of personal status, marriage, and inheritance under the Maliki school of thought.\n\nThe courts apply these systems concurrently, with the hierarchy of laws generally placing the Constitution at the apex, followed by enacted laws, and then customary/Islamic law. The interaction of these systems creates a unique, pluralistic legal order that accommodates Nigeria's diverse cultural and religious landscape."
            };

            const TOTAL_QUESTIONS = 5;
            const MAX_SUBMISSIONS = 3;
            const TIMER_DURATION = 1800; // 30 minutes

            // Exam state – persisted in localStorage
            let state = {
                submitted: { 1: false, 2: false, 3: false, 4: false, 5: false },
                started: { 1: false, 2: false, 3: false, 4: false, 5: false },
                timeLeft: { 1: TIMER_DURATION, 2: TIMER_DURATION, 3: TIMER_DURATION, 4: TIMER_DURATION,
                5: TIMER_DURATION },
                submittedCount: 0,
                allRevealed: false,
                examEnded: false,
                answers: { 1: '', 2: '', 3: '', 4: '', 5: '' },
                timerStart: { 1: null, 2: null, 3: null, 4: null, 5: null },
                timerRunning: { 1: false, 2: false, 3: false, 4: false, 5: false },
                correctionShown: { 1: false, 2: false, 3: false, 4: false, 5: false },
                hiddenQuestions: { 1: false, 2: false, 3: false, 4: false, 5: false }
            };

            let history = [];
            const intervals = {};
            const dom = {};

            function saveState() {
                try { localStorage.setItem('examState', JSON.stringify(state)); } catch (_) {}
            }

            function loadState() {
                try {
                    const raw = localStorage.getItem('examState');
                    if (raw) {
                        const parsed = JSON.parse(raw);
                        state = { ...state, ...parsed };
                        for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                            if (!state.submitted[q]) state.submitted[q] = false;
                            if (!state.started[q]) state.started[q] = false;
                            if (!state.timeLeft[q]) state.timeLeft[q] = TIMER_DURATION;
                            if (!state.answers[q]) state.answers[q] = '';
                            if (!state.timerStart[q]) state.timerStart[q] = null;
                            if (!state.timerRunning[q]) state.timerRunning[q] = false;
                            if (!state.correctionShown[q]) state.correctionShown[q] = false;
                            if (!state.hiddenQuestions[q]) state.hiddenQuestions[q] = false;
                        }
                        return true;
                    }
                } catch (_) {}
                return false;
            }

            function saveHistory() {
                try { localStorage.setItem('examHistory', JSON.stringify(history)); } catch (_) {}
            }

            function loadHistory() {
                try {
                    const raw = localStorage.getItem('examHistory');
                    if (raw) { history = JSON.parse(raw); return true; }
                } catch (_) {}
                return false;
            }

            function initDomRefs() {
                for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                    dom['timer' + q] = document.getElementById('timer' + q);
                    dom['timerInline' + q] = document.getElementById('timer' + q + '_inline');
                    dom['answer' + q] = document.getElementById('answer' + q);
                    dom['question' + q] = document.getElementById('q' + q);
                    dom['correction' + q] = document.getElementById('correction' + q);
                    dom['startBtn' + q] = document.querySelector('.btn-start[data-q="' + q + '"]');
                    dom['submitBtn' + q] = document.querySelector('.btn-submit[data-q="' + q + '"]');
                }
                dom.count = document.getElementById('count');
                dom.statusMsg = document.getElementById('statusMsg');
                dom.historyBadge = document.getElementById('historyBadge');
                dom.historyContainer = document.getElementById('historyListContainer');
                dom.restartBtn = document.getElementById('restartBtn');
                dom.clearAllHistory = document.getElementById('clearAllHistory');
                dom.toast = document.getElementById('toast');
                dom.navLinks = document.querySelectorAll('.nav-links a[data-view]');
                dom.views = {
                    home: document.getElementById('view-home'),
                    history: document.getElementById('view-history')
                };
            }

            let toastTimer = null;

            function showToast(message, type) {
                const el = dom.toast;
                if (!el) return;
                el.textContent = message;
                el.className = 'toast';
                if (type) el.classList.add(type);
                el.classList.add('show');
                clearTimeout(toastTimer);
                toastTimer = setTimeout(() => { el.classList.remove('show'); }, 3500);
            }

            function switchView(viewName) {
                dom.navLinks.forEach(link => {
                    const v = link.dataset.view;
                    link.classList.toggle('active', v === viewName);
                });
                Object.keys(dom.views).forEach(key => {
                    dom.views[key].classList.toggle('active', key === viewName);
                });
                if (viewName === 'history') renderHistory();
            }

            function updateTimerDisplay(q, seconds) {
                const mins = String(Math.floor(seconds / 60)).padStart(2, '0');
                const secs = String(seconds % 60).padStart(2, '0');
                const display = mins + ':' + secs;
                if (dom['timer' + q]) dom['timer' + q].textContent = display;
                if (dom['timerInline' + q]) dom['timerInline' + q].textContent = display;
            }

            function stopTimer(q) {
                if (intervals[q]) { clearInterval(intervals[q]);
                    delete intervals[q]; }
                state.timerRunning[q] = false;
                state.timerStart[q] = null;
                saveState();
            }

            function startTimer(q) {
                if (intervals[q]) stopTimer(q);
                if (state.submitted[q]) return;
                if (state.timeLeft[q] <= 0) { handleTimeUp(q); return; }

                state.timerRunning[q] = true;
                state.timerStart[q] = Date.now();
                saveState();
                updateTimerDisplay(q, state.timeLeft[q]);

                intervals[q] = setInterval(() => {
                    if (!state.timerRunning[q]) return;
                    const elapsed = Math.floor((Date.now() - state.timerStart[q]) / 1000);
                    let remaining = Math.max(0, state.timeLeft[q] - elapsed);
                    updateTimerDisplay(q, remaining);
                    if (remaining <= 0) {
                        stopTimer(q);
                        handleTimeUp(q);
                    }
                }, 500);
            }

            function restoreTimers() {
                for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                    if (state.started[q] && !state.submitted[q] && state.timerRunning[q] && state.timerStart[q]) {
                        const elapsed = Math.floor((Date.now() - state.timerStart[q]) / 1000);
                        let remaining = state.timeLeft[q] - elapsed;
                        if (remaining <= 0) {
                            state.timeLeft[q] = 0;
                            saveState();
                            handleTimeUp(q);
                        } else {
                            state.timeLeft[q] = remaining;
                            saveState();
                            startTimer(q);
                        }
                    } else if (state.started[q] && !state.submitted[q] && state.timeLeft[q] > 0) {
                        state.timerRunning[q] = true;
                        state.timerStart[q] = Date.now();
                        saveState();
                        startTimer(q);
                    } else if (state.submitted[q] || state.timeLeft[q] <= 0) {
                        lockQuestion(q, state.timeLeft[q] <= 0);
                    }
                }
            }

            function lockQuestion(q, isTimeUp) {
                const ta = dom['answer' + q];
                const startBtn = dom['startBtn' + q];
                const submitBtn = dom['submitBtn' + q];
                if (ta) ta.disabled = true;
                if (startBtn) startBtn.disabled = true;
                if (submitBtn) submitBtn.disabled = true;
                if (isTimeUp) {
                    const timerEl = dom['timer' + q];
                    if (timerEl) { timerEl.textContent = '⏰ TIME UP';
                        timerEl.classList.add('timeup'); }
                    const timerInline = dom['timerInline' + q];
                    if (timerInline) { timerInline.textContent = '⏰ TIME UP';
                        timerInline.classList.add('timeup'); }
                }
                if (state.submitted[q]) {
                    if (state.correctionShown[q] || state.allRevealed) showCorrection(q);
                }
                saveState();
            }

            function handleTimeUp(q) {
                if (state.submitted[q]) return;
                state.timeLeft[q] = 0;
                state.submitted[q] = true;
                state.submittedCount++;
                const ta = dom['answer' + q];
                if (ta) state.answers[q] = ta.value;
                addHistoryEntry(q, state.answers[q], CORRECTIONS[q] || 'Model answer not available.');
                saveState();
                lockQuestion(q, true);
                updateCount();
                checkAndEndExam();
                showToast('⏰ Time up for Question ' + q + '. Auto‑submitted.', 'warning');
            }

            function startExam(q) {
                if (state.submitted[q]) {
                    showToast('This question has already been submitted.', 'error');
                    return;
                }
                if (state.started[q]) {
                    showToast('Timer already running for this question.', 'warning');
                    return;
                }
                if (state.submittedCount >= MAX_SUBMISSIONS) {
                    showToast('You have already answered the maximum of ' + MAX_SUBMISSIONS + ' questions.', 'error');
                    return;
                }
                for (let i = 1; i <= TOTAL_QUESTIONS; i++) {
                    if (i !== q && state.started[i] && !state.submitted[i]) {
                        showToast('You must submit Question ' + i + ' before starting another question.', 'error');
                        return;
                    }
                }

                state.started[q] = true;
                state.timerRunning[q] = true;
                state.timerStart[q] = Date.now();
                const ta = dom['answer' + q];
                if (ta) { ta.disabled = false;
                    ta.value = state.answers[q] || '';
                    ta.focus(); }
                if (dom['startBtn' + q]) dom['startBtn' + q].disabled = true;
                if (dom['submitBtn' + q]) dom['submitBtn' + q].disabled = false;
                state.timeLeft[q] = TIMER_DURATION;
                updateTimerDisplay(q, TIMER_DURATION);
                saveState();
                startTimer(q);
                showToast('⏱️ Timer started for Question ' + q, 'success');
            }

            function handleSubmit(q) {
                if (state.submitted[q]) {
                    showToast('This question has already been submitted.', 'error');
                    return;
                }
                if (!state.started[q]) {
                    showToast('Please start the exam for this question first.', 'error');
                    return;
                }
                const ta = dom['answer' + q];
                const answerText = ta ? ta.value : '';
                if (!answerText.trim()) {
                    showToast('Please write your answer before submitting.', 'warning');
                    return;
                }

                stopTimer(q);
                state.submitted[q] = true;
                state.submittedCount++;
                state.answers[q] = answerText;
                addHistoryEntry(q, answerText, CORRECTIONS[q] || 'Model answer not available.');
                saveState();
                lockQuestion(q, false);
                updateCount();
                checkAndEndExam();
                showToast('✅ Question ' + q + ' submitted successfully!', 'success');
            }

            function showCorrection(q) {
                const corrDiv = dom['correction' + q];
                if (!corrDiv) return;
                corrDiv.textContent = CORRECTIONS[q] || '✔ Model answer not available.';
                corrDiv.classList.add('show');
                state.correctionShown[q] = true;
                saveState();
            }

            let historyIdCounter = 0;

            function addHistoryEntry(question, answer, correction) {
                const entry = {
                    id: ++historyIdCounter,
                    question: question,
                    answer: answer,
                    correction: correction,
                    timestamp: new Date().toLocaleString('en-NG', {
                        day: '2-digit',
                        month: 'short',
                        year: 'numeric',
                        hour: '2-digit',
                        minute: '2-digit',
                        second: '2-digit',
                        hour12: true
                    })
                };
                history.unshift(entry);
                saveHistory();
                updateHistoryBadge();
            }

            function deleteHistoryEntry(id) {
                history = history.filter(e => e.id !== id);
                saveHistory();
                updateHistoryBadge();
                renderHistory();
                showToast('Entry deleted.', 'success');
            }

            function clearAllHistory() {
                if (history.length === 0) { showToast('History is already empty.', 'warning'); return; }
                if (confirm('Are you sure you want to delete ALL history entries?')) {
                    history = [];
                    saveHistory();
                    updateHistoryBadge();
                    renderHistory();
                    showToast('All history cleared.', 'success');
                }
            }

            function updateHistoryBadge() {
                if (dom.historyBadge) dom.historyBadge.textContent = history.length;
            }

            function renderHistory() {
                const container = dom.historyContainer;
                if (!container) return;
                if (history.length === 0) {
                    container.innerHTML =
                        `
                        <div class="history-empty">
                            📭 No practice history yet.
                            <p>Submit answers during practice sessions and they will appear here.</p>
                        </div>
                    `;
                    return;
                }
                let html = '<div class="history-list">';
                history.forEach(entry => {
                    const answerPreview = entry.answer.length > 300 ? entry.answer.slice(0, 300) + '…' : entry.answer;
                    html += `
                        <div class="history-card" data-id="${entry.id}">
                            <div class="hcard-head">
                                <span class="hq">📌 Question ${entry.question}</span>
                                <span class="hdate">${entry.timestamp}</span>
                                <div class="hactions">
                                    <button class="btn-view" data-id="${entry.id}">👁️ View</button>
                                    <button class="btn-delete" data-id="${entry.id}">🗑️ Delete</button>
                                </div>
                            </div>
                            <div class="hcard-body">
                                <div class="answer-preview" id="hanswer_${entry.id}">${escapeHtml(answerPreview)}</div>
                                <div class="correction-preview" id="hcorr_${entry.id}">${escapeHtml(entry.correction)}</div>
                                <button class="toggle-correction" data-id="${entry.id}">📖 Show Correction</button>
                            </div>
                        </div>
                    `;
                });
                html += '</div>';
                container.innerHTML = html;

                container.querySelectorAll('.btn-view').forEach(btn => {
                    btn.addEventListener('click', function() {
                        const id = parseInt(this.dataset.id);
                        const card = this.closest('.history-card');
                        const preview = card.querySelector('.answer-preview');
                        const corr = card.querySelector('.correction-preview');
                        const toggleBtn = card.querySelector('.toggle-correction');
                        if (preview) { preview.classList.toggle('expanded');
                            this.textContent = preview.classList.contains('expanded') ? '👁️ Hide' :
                            '👁️ View'; }
                        if (corr) { corr.classList.toggle('show'); }
                        if (toggleBtn) { toggleBtn.textContent = corr.classList.contains('show') ?
                                '📖 Hide Correction' : '📖 Show Correction'; }
                    });
                });

                container.querySelectorAll('.btn-delete').forEach(btn => {
                    btn.addEventListener('click', function() {
                        const id = parseInt(this.dataset.id);
                        deleteHistoryEntry(id);
                    });
                });

                container.querySelectorAll('.toggle-correction').forEach(btn => {
                    btn.addEventListener('click', function() {
                        const id = parseInt(this.dataset.id);
                        const card = this.closest('.history-card');
                        const corr = card.querySelector('.correction-preview');
                        if (corr) {
                            corr.classList.toggle('show');
                            this.textContent = corr.classList.contains('show') ? '📖 Hide Correction' :
                                '📖 Show Correction';
                        }
                    });
                });
            }

            function escapeHtml(text) {
                const div = document.createElement('div');
                div.textContent = text;
                return div.innerHTML;
            }

            function checkAndEndExam() {
                if (state.allRevealed || state.examEnded) return;
                let count = 0;
                for (let i = 1; i <= TOTAL_QUESTIONS; i++) {
                    if (state.submitted[i]) count++;
                }
                if (count >= MAX_SUBMISSIONS) {
                    revealAllAnswers();
                    hideUnanswered();
                }
            }

            function revealAllAnswers() {
                if (state.allRevealed) return;
                state.allRevealed = true;
                for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                    if (state.submitted[q]) showCorrection(q);
                }
                saveState();
                dom.statusMsg.textContent = '✅ All answers revealed.';
                showToast('🎓 Exam complete! Corrections are now visible.', 'success');
            }

            function hideUnanswered() {
                if (state.examEnded) return;
                state.examEnded = true;
                for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                    if (!state.submitted[q]) {
                        const el = dom['question' + q];
                        if (el) { el.classList.add('hidden');
                            state.hiddenQuestions[q] = true; }
                    }
                }
                for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                    if (dom['startBtn' + q]) dom['startBtn' + q].disabled = true;
                    if (dom['submitBtn' + q]) dom['submitBtn' + q].disabled = true;
                }
                saveState();
                dom.statusMsg.textContent = '✅ Exam ended. Only answered questions remain.';
            }

            function updateCount() {
                if (dom.count) dom.count.textContent = state.submittedCount;
                if (state.submittedCount >= MAX_SUBMISSIONS) {
                    dom.statusMsg.textContent = '✅ Maximum questions answered. Exam complete.';
                } else {
                    const remaining = MAX_SUBMISSIONS - state.submittedCount;
                    dom.statusMsg.textContent = '📝 ' + remaining + ' more question' + (remaining > 1 ? 's' : '') +
                        ' to complete.';
                }
            }

            function restartExam() {
                if (!confirm('⚠️ Restarting will reset all exam progress. History will be preserved. Continue?')) return;
                for (let q = 1; q <= TOTAL_QUESTIONS; q++) stopTimer(q);
                state.submitted = { 1: false, 2: false, 3: false, 4: false, 5: false };
                state.started = { 1: false, 2: false, 3: false, 4: false, 5: false };
                state.timeLeft = { 1: TIMER_DURATION, 2: TIMER_DURATION, 3: TIMER_DURATION, 4: TIMER_DURATION,
                5: TIMER_DURATION };
                state.submittedCount = 0;
                state.allRevealed = false;
                state.examEnded = false;
                state.answers = { 1: '', 2: '', 3: '', 4: '', 5: '' };
                state.timerStart = { 1: null, 2: null, 3: null, 4: null, 5: null };
                state.timerRunning = { 1: false, 2: false, 3: false, 4: false, 5: false };
                state.correctionShown = { 1: false, 2: false, 3: false, 4: false, 5: false };
                state.hiddenQuestions = { 1: false, 2: false, 3: false, 4: false, 5: false };
                saveState();

                for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                    const ta = dom['answer' + q];
                    if (ta) { ta.value = '';
                        ta.disabled = true; }
                    const timerEl = dom['timer' + q];
                    if (timerEl) { timerEl.textContent = '30:00';
                        timerEl.classList.remove('timeup'); }
                    const timerInline = dom['timerInline' + q];
                    if (timerInline) { timerInline.textContent = '30:00';
                        timerInline.classList.remove('timeup'); }
                    if (dom['startBtn' + q]) dom['startBtn' + q].disabled = false;
                    if (dom['submitBtn' + q]) dom['submitBtn' + q].disabled = true;
                    const corrDiv = dom['correction' + q];
                    if (corrDiv) { corrDiv.textContent = '';
                        corrDiv.classList.remove('show'); }
                    const qEl = dom['question' + q];
                    if (qEl) qEl.classList.remove('hidden');
                }
                updateCount();
                dom.statusMsg.textContent = '🔄 Exam restarted. Start a question to begin.';
                showToast('🔄 Exam has been restarted.', 'success');
                switchView('home');
            }

            function init() {
                initDomRefs();
                loadHistory();
                updateHistoryBadge();
                const hasState = loadState();

                for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                    const ta = dom['answer' + q];
                    if (ta) {
                        ta.value = state.answers[q] || '';
                        ta.disabled = state.submitted[q] || state.examEnded || state.timeLeft[q] <= 0;
                    }
                    if (state.submitted[q] || state.timeLeft[q] <= 0) {
                        lockQuestion(q, state.timeLeft[q] <= 0);
                    } else if (state.started[q]) {
                        if (dom['startBtn' + q]) dom['startBtn' + q].disabled = true;
                        if (dom['submitBtn' + q]) dom['submitBtn' + q].disabled = false;
                        if (ta) ta.disabled = false;
                    }
                    if (state.timeLeft[q] !== undefined) updateTimerDisplay(q, state.timeLeft[q]);
                    if (state.timeLeft[q] <= 0 && state.submitted[q]) {
                        const timerEl = dom['timer' + q];
                        if (timerEl) { timerEl.textContent = '⏰ TIME UP';
                            timerEl.classList.add('timeup'); }
                        const timerInline = dom['timerInline' + q];
                        if (timerInline) { timerInline.textContent = '⏰ TIME UP';
                            timerInline.classList.add('timeup'); }
                    }
                    if (state.correctionShown[q] || state.allRevealed) {
                        if (state.submitted[q]) showCorrection(q);
                    }
                    if (state.hiddenQuestions[q] || (state.examEnded && !state.submitted[q])) {
                        const el = dom['question' + q];
                        if (el) el.classList.add('hidden');
                    }
                }

                updateCount();
                if (state.examEnded) hideUnanswered();
                if (state.allRevealed) {
                    for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                        if (state.submitted[q]) showCorrection(q);
                    }
                    dom.statusMsg.textContent = '✅ All answers revealed.';
                }
                restoreTimers();

                document.querySelectorAll('.btn-start').forEach(btn => {
                    btn.addEventListener('click', function() {
                        const q = parseInt(this.dataset.q);
                        startExam(q);
                    });
                });

                document.querySelectorAll('.btn-submit').forEach(btn => {
                    btn.addEventListener('click', function() {
                        const q = parseInt(this.dataset.q);
                        handleSubmit(q);
                    });
                });

                dom.navLinks.forEach(link => {
                    link.addEventListener('click', function(e) {
                        e.preventDefault();
                        switchView(this.dataset.view);
                    });
                });

                dom.restartBtn.addEventListener('click', restartExam);
                dom.clearAllHistory.addEventListener('click', clearAllHistory);

                window.addEventListener('beforeunload', function() {
                    for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                        const ta = dom['answer' + q];
                        if (ta && !state.submitted[q]) state.answers[q] = ta.value;
                    }
                    saveState();
                });

                for (let q = 1; q <= TOTAL_QUESTIONS; q++) {
                    const ta = dom['answer' + q];
                    if (ta) {
                        ta.addEventListener('input', function() {
                            if (!state.submitted[q]) { state.answers[q] = this.value;
                                saveState(); }
                        });
                    }
                }

                if (state.submittedCount > 0) {
                    showToast('📚 Welcome back! Your progress has been restored.', 'success');
                } else {
                    showToast('📖 Ready to practice? Start a question when you\'re ready.', 'success');
                }
                updateHistoryBadge();
                console.log('🏛️ LAW 132 · LEGAL METHODS II Exam System initialized.');
            }

            document.addEventListener('DOMContentLoaded', init);

        })();